<%= form_for(@product_related_product) do |f| %>
  <% if @product_related_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_related_product.errors.count, "error") %> prohibited this product_related_product from being saved:</h2>

      <ul>
      <% @product_related_product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :product_id %><br>
    <%= f.number_field :product_id %>
  </div>
  <div class="field">
    <%= f.label :related_prooduct %><br>
    <%= f.number_field :related_prooduct %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
